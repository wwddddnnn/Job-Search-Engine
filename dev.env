# 本机开发环境（由 Hermes 生成；机器相关，换机器需要改这一行）
#
# codex-hermes-loop.sh 会 source 本文件；同名环境变量优先级更高，
# 也就是说 `PYBIN=/path/to/python ./codex-hermes-loop.sh "任务"` 可以临时覆盖。
#
# conda 环境：Job-Search-Engine（Python 3.12.14）
# 创建命令（本机记录）：
#   conda create -n Job-Search-Engine python=3.12 -y --solver classic
# 注意：本机 conda 的 libmamba 求解器插件是坏的，必须显式加 --solver classic。
PYBIN=/opt/homebrew/Caskroom/miniconda/base/envs/Job-Search-Engine/bin/python
